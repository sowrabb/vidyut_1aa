// Deprecated: Provider-based app providers have been migrated to Riverpod.
// Use providers in `app/provider_registry.dart` instead.

@Deprecated('Use Riverpod providers in app/provider_registry.dart')
List<dynamic> buildAppProviders() {
  return const [];
}
